<header class="header">
  <nav role="navbar navbar-inverse bg-inverse navbar-fixed-top">

<span class="brand">
  <%= link "postModern", to: dashboard_path(@conn, :index), class: "h3" %>
</span>

    <div class="dropdown float-right">
    <button class="btn btn-dropdown dropdown-toggle" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">

    <%#   <%= if @current_user == !nil do %>
    <%#     <%= @current_user.display_name %>
    <%#       <% else %>

      <%= unless Guardian.Plug.authenticated?(@conn, []) do %>
           User Menu
      <% else %>
          <p>Logged In</p>
      <% end %>
    <%#       <% end %>
    </button>
    <ul class="dropdown-menu" role="menu" aria-labelledby="dropdownMenuButton">
      <%= if Guardian.Plug.authenticated?(@conn, []) do %>

      <li class="dropdown"><%= link "New Article", to: article_path(@conn, :new), class: "dropdown-item" %></li>
      <li class="divider"></li>
      <li class="dropdown"><%= link "Logout", to: session_path(@conn, :logout), method:
      :post,  class: "dropdown-item"  %></li>
      <% else %>

    <li class="dropdown"><%= link "Sign In", to: session_path(@conn, :index), class: "dropdown-item"  %></li>
      <li class="dropdown"><%= link "Sign Up", to: user_path(@conn, :new), class: "dropdown-item"  %></li>
      <% end %>
    </ul>
  </div>


  </nav>
</header>
